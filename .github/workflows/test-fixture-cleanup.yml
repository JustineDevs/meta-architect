name: Test Fixture Cleanup

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Prune test fixtures
        run: bash ./scripts/cleanup-test-fixtures.sh
